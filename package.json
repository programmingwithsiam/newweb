{
  "name": "siam-ai-portfolio",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  }
  ,"scripts": {
    "dev:app": "npm --prefix app run dev",
    "build:app": "npm --prefix app run build",
    "preview:app": "npm --prefix app run preview"
  }
}